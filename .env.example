# RODA/ETERNA backend URL
RODA_API_URL=http://localhost:8080

# Anonym sökning kräver inga credentials i portalen. Oautentiserade anrop
# skickas vidare till RODA som gäst-användaren "guest" — se till att
# gruppen "guests" i RODA har rollerna aip.read, descriptive_metadata.read
# och representation.read (Administration → Användare och grupper).
